<!-- show.html.erb -->
<div class="container">
  <p>Location Name: <%= @location.name %></p>
  <p>Location Address: <%= @location.address %></p>
  <p>Assigned Engineer/s: </p>
    <ul>
      <% @location.users.each do |user| %>
        <li><%= user.name %></li>
      <% end %>
    </ul>

  <%= link_to "Update Current Qty", location_update_materials_path(@location), method: :post, data: { turbo_method: :post }, class: "btn btn-primary" %>
</div>
